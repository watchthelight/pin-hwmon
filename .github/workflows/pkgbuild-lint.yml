name: PKGBUILD Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          # namcap is Arch-specific, so skip or use alternative
      - name: Run shellcheck
        run: |
          find . -name "*.sh" -exec shellcheck {} \;
      - name: Lint PKGBUILD
        run: |
          # Placeholder for namcap if available
          echo "PKGBUILD linting would run here with namcap"
