post_install() {
  cat << EOF
To enable the pin-hwmon service, run:
  sudo systemctl enable --now pin-hwmon.service

To test the installation, run:
  cat /etc/hwmon/cpu_tctl && awk '{printf "%.1f°C\n", \$1/1000}' /etc/hwmon/cpu_tctl
EOF
}

post_upgrade() {
  post_install
}
